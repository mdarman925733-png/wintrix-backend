name: Build Release AAB

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Decode Keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > $GITHUB_WORKSPACE/app/ishqana-release.jks

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Remove local aapt2 override for CI
        run: sed -i '/android.aapt2FromMavenOverride/d' gradle.properties

      - name: Build Release Bundle
        run: |
          ./gradlew :app:bundleRelease \
            -Pandroid.injected.signing.store.file=$GITHUB_WORKSPACE/app/ishqana-release.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }} \
            -Pandroid.injected.signing.store.type=jks

      - name: Print merged manifest (verify permissions)
        run: |
          echo "----- MERGED MANIFEST -----"
          cat app/build/intermediates/merged_manifest/release/AndroidManifest.xml || \
          cat app/build/intermediates/merged_manifests/release/AndroidManifest.xml
          echo "----------------------------"
          echo "Checking for FOREGROUND_SERVICE_MEDIA_PROJECTION..."
          grep -i "FOREGROUND_SERVICE_MEDIA_PROJECTION" app/build/intermediates/merged_manifest*/release/AndroidManifest.xml || echo "NOT FOUND - permission successfully removed"

      - name: Upload merged manifest
        uses: actions/upload-artifact@v4
        with:
          name: merged-manifest
          path: app/build/intermediates/merged_manifest*/release/AndroidManifest.xml

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: release-bundle
          path: app/build/outputs/bundle/release/*.aab
